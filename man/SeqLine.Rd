% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqLine.R
\name{SeqLine}
\alias{SeqLine}
\title{SeqLine R6 Class}
\usage{
SeqLine(...)
}
\arguments{
\item{gr}{A `GRanges` object containing genomic intervals.}

\item{yCol}{Optional column name in `gr` for y-values.}

\item{aesthetics}{Optional list of aesthetic overrides.}

\item{layout_track}{A list of panel layout metadata for a track.}

\item{track_windows}{A `GRanges` object defining genomic windows.}
}
\value{
A new `SeqLine` object.
}
\description{
R6 class for drawing line plots from genomic intervals in the SeqPlot
framework. Inherits from [SeqElement].

Create a new `SeqLine` object.

Prepare line coordinates in canvas space for each genomic window.

Draw lines onto the plotting canvas.
}
\details{
Each genomic interval is represented as a point at its midpoint by default,
with y-values taken from a metadata column or set to a constant. The points
are connected into a continuous line. Step lines can also be drawn by setting
the `type` aesthetic to `"s"` or `"step"`.
}
\section{Fields}{

\describe{
\item{\code{gr}}{A `GRanges` object containing genomic intervals.}

\item{\code{y}}{Numeric vector of y-values (from `yCol` or constant).}

\item{\code{yCol}}{Optional column name in `gr` used for y-values.}

\item{\code{coordOriginal}}{A `GRanges` object storing unmodified input coordinates.}

\item{\code{coordCanvas}}{List of matrices storing transformed line coordinates
in canvas space for each genomic window.}

\item{\code{aesthetics}}{List of current aesthetics merged with defaults.}

\item{\code{defaultAesthetics}}{Default aesthetics for lines:
\code{type = "n"}, \code{size = 0.1}, \code{color = "#1C1B1A"}.}
}}

\examples{
gr <- GenomicRanges::GRanges(
  "chr1",
  IRanges::IRanges(c(1, 100, 200), width = 50),
  score = c(2, 5, 3)
)
line <- SeqLine(gr, yCol = "score")
line$prep(layout_track = some_layout, track_windows = some_windows)
line$draw()

}
