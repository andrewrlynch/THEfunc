% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqLink.R
\name{SeqLink}
\alias{SeqLink}
\title{SeqLink R6 Class}
\usage{
SeqLink(...)
}
\arguments{
\item{gr1}{Deprecated start positions; use `x1` instead.}

\item{gr2}{Deprecated end positions; use `x2` instead.}

\item{x1}{Preferred: `GRanges` object giving start positions of links.}

\item{x2}{Preferred: `GRanges` object giving end positions of links.}

\item{t0}{Integer or vector giving track indices for start loci (default: `0`).}

\item{t1}{Integer or vector giving track indices for end loci (default: `0`).}

\item{y0}{Numeric or vector giving vertical start positions (default: `0`).}

\item{y1}{Numeric or vector giving vertical end positions (default: `0`).}

\item{color}{Color for links (currently unused in base class).}

\item{orientation}{Character or vector specifying link orientation
(default: `"*"`).}
}
\value{
A new `SeqLink` object.
}
\description{
R6 class representing a link between two genomic loci in the SeqPlot
framework. Inherits from [SeqElement].

Create a new `SeqLink` object.
}
\details{
A `SeqLink` connects two sets of genomic intervals (`gr1` and `gr2`),
optionally across different tracks. It stores track indices (`t0`, `t1`),
vertical anchor positions (`y0`, `y1`), and link orientations. Higher-level
link classes such as `SeqArch` extend this class to implement specific
visualization styles (arches, lines, bezier curves, etc.).
}
\section{Fields}{

\describe{
\item{\code{gr1}}{A `GRanges` object containing the start loci of each link.}

\item{\code{gr2}}{A `GRanges` object containing the end loci of each link.}

\item{\code{t0}}{Integer vector giving the track indices of the start loci.}

\item{\code{t1}}{Integer vector giving the track indices of the end loci.}

\item{\code{y0}}{Numeric vector of vertical anchor positions for the start loci.}

\item{\code{y1}}{Numeric vector of vertical anchor positions for the end loci.}

\item{\code{orientation}}{Character vector specifying orientation of each link
(e.g. `"+"`, `"-"`, `"*"`).}
}}

\examples{
gr1 <- GenomicRanges::GRanges("chr1", IRanges::IRanges(c(10, 50), width = 1))
gr2 <- GenomicRanges::GRanges("chr1", IRanges::IRanges(c(100, 150), width = 1))
link <- SeqLink(gr1, gr2, t0 = 1, t1 = 1, y0 = 0, y1 = 0, orientation = "+")
link$gr1

}
