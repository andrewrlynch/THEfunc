% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqArea.R
\name{SeqArea}
\alias{SeqArea}
\title{SeqArea R6 Class}
\usage{
SeqArea(...)
}
\arguments{
\item{gr}{A `GRanges` object containing genomic intervals.}

\item{yCol}{Optional column name in `gr` for y-values.}

\item{groupCol}{Optional column name in `gr` for grouping areas.}

\item{groupLevels}{Optional vector of group levels to enforce order.}

\item{aesthetics}{Optional list of aesthetic overrides.}

\item{layout_track}{A list of panel layout metadata for a track.}

\item{track_windows}{A `GRanges` object defining genomic windows.}
}
\value{
A new `SeqArea` object.
}
\description{
R6 class for drawing filled area plots from genomic intervals in the SeqPlot
framework. Inherits from [SeqElement].

Create a new `SeqArea` object.

Prepare stacked area polygons in canvas space for each genomic window.

Draw stacked areas onto the plotting canvas.
}
\details{
Each genomic interval is represented as an area under a line, with y-values
taken from a metadata column or set to a constant. Areas can be grouped and
stacked, with each group assigned its own fill color. Missing group–x
combinations are padded with zero values to ensure continuous stacked shapes.
}
\section{Fields}{

\describe{
\item{\code{gr}}{A `GRanges` object containing genomic intervals.}

\item{\code{yCol}}{Optional column name in `gr` used for y-values.}

\item{\code{groupCol}}{Optional column name in `gr` defining groups for stacked areas.}

\item{\code{groupLevels}}{Optional character vector specifying factor levels for groups.}

\item{\code{y}}{Numeric vector of y-values (from `yCol` or constant).}

\item{\code{group}}{Factor defining group membership of each interval.}

\item{\code{aesthetics}}{List of current aesthetics merged with defaults.}

\item{\code{coordCanvas}}{List of polygon coordinate lists for each group in
each window, storing transformed x, y, and fill values.}

\item{\code{yStackedMax}}{Maximum stacked y-value across groups, used for scaling.}

\item{\code{defaultAesthetics}}{Default aesthetics for area drawing:
\code{fill = "grey60"}, \code{col = "black"}, \code{alpha = 1}, \code{lwd = 0.5}.}
}}

\examples{
gr <- GenomicRanges::GRanges(
  "chr1",
  IRanges::IRanges(c(1, 100, 200), width = 50),
  score = c(2, 5, 3),
  group = c("A", "B", "A")
)
area <- SeqArea(gr, yCol = "score", groupCol = "group")
area$prep(layout_track = some_layout, track_windows = some_windows)
area$draw()

}
