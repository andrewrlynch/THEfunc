% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqRecon.R
\name{SeqRecon}
\alias{SeqRecon}
\title{SeqRecon R6 Class}
\usage{
SeqRecon(...)
}
\arguments{
\item{gr1}{Deprecated start loci; use `x1` instead.}

\item{gr2}{Deprecated end loci; use `x2` instead.}

\item{x1}{Preferred: `GRanges` object with start loci.}

\item{x2}{Preferred: `GRanges` object with end loci.}

\item{yCol}{Optional column name for custom arch heights.}

\item{y0, y1}{Numeric start and end y-values (default: `0`).}

\item{t0, t1}{Track indices for start and end loci (default: `0` → current track).}

\item{orientation}{Character vector of orientations (default: `"*"`).}

\item{curve}{Character curvature mode (`"length"`, `"equal"`, or numeric).}

\item{drawClasses}{Which class tiers to render.}

\item{aesthetics}{List of aesthetic overrides (colors, widths, etc.).}

\item{layout_all_tracks}{List of layout panels for all tracks.}

\item{track_windows_list}{List of genomic windows per track.}

\item{arc_track_idx}{Index of the arc track in which arches are drawn.}
}
\value{
A new `SeqRecon` object.
}
\description{
R6 class for plotting **ReCon-style** structural variant arches
(inversions, duplications/deletions, and translocations).
Inherits from [SeqArch].

Create a new `SeqRecon` object.

Prepare classification and layout for ReCon-style drawing.

Draw ReCon-style arches, tiers, and labels.
}
\details{
`SeqRecon` extends `SeqArch` by automatically classifying structural
variants into three tiers:

* **Inversion** (`+/+`, `-/-`) → head-to-head (HH) or tail-to-tail (TT).
* **Duplication/Deletion** (`-/+`, `+/-`) → tandem duplication or deletion.
* **Translocation** (different chromosomes).

Each class is mapped to a fixed vertical tier and colored consistently.
Tier guide lines and labels are drawn automatically.
}
\section{Fields}{

\describe{
\item{\code{last_arc_track}}{Cached layout of the last arc track used in `prep()`.}

\item{\code{col_h2h}}{Color for head-to-head inversions (`+/+`).}

\item{\code{col_t2t}}{Color for tail-to-tail inversions (`-/-`).}

\item{\code{col_dup}}{Color for tandem duplications (`-/+`).}

\item{\code{col_del}}{Color for deletions (`+/-`).}

\item{\code{col_trans}}{Color for translocations (across chromosomes).}

\item{\code{drawClasses}}{Character vector of class names to draw
(default: `c("Inversion", "Dup/Del", "Translocation")`).}

\item{\code{arc_track_idx}}{Integer index of the track where arcs are drawn.}

\item{\code{layout_all_tracks}}{Cached layout for all tracks used in `prep()`.}

\item{\code{tierMultipliers}}{Numeric vector assigning each variant to a fixed tier.}
}}

\examples{
gr1 <- GenomicRanges::GRanges("chr1", IRanges::IRanges(c(10, 50), width = 1), strand = c("+", "-"))
gr2 <- GenomicRanges::GRanges("chr1", IRanges::IRanges(c(100, 150), width = 1), strand = c("+", "-"))
recon <- SeqRecon(gr1, gr2)
# recon$prep(layout_all_tracks = some_layout, track_windows_list = some_windows, arc_track_idx = 1)
# recon$draw()

}
