% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqTile.R
\name{SeqTile}
\alias{SeqTile}
\title{SeqTile R6 Class}
\usage{
SeqTile(...)
}
\arguments{
\item{gr}{Deprecated primary `GRanges` (use `x=`).}

\item{x}{Primary `GRanges` for x-axis positions.}

\item{y}{Optional `GRanges` for genomic y-axis positions (must
have length equal to `x` or `gr`).}

\item{groupCol}{Column name in `gr` metadata for grouping (y-axis).}

\item{aesthetics}{List of aesthetics (`border`, `lwd`).}

\item{aes}{A `SeqAes` object from `aes()`. Use `y` to map a
column to discrete y-axis categories and `fill` to map a
column to tile fill color.}

\item{scale}{A `SeqScale` color scale for the fill mapping.}

\item{layout_track}{A list of panel metadata for the current track.}

\item{track_windows}{A `GRanges` of genomic windows for the track.}
}
\description{
An R6 class for plotting categorical heatmap-style tiles.
Genomic ranges define the x-axis, and a categorical grouping column
defines the y-axis. Each tile is filled with the color stored in the
`color` metadata column of the GRanges.

Create a new `SeqTile` object.
Backwards compatibility: accepts `gr=` or `x=` for primary
genomic ranges. Optionally accepts `y=` as a `GRanges` to
enable genomic y-axis (2D mode). Otherwise use `aes(y=...)`
or `groupCol` to define discrete categories on the y-axis.

Prepare tile coordinates for plotting in canvas space.

Draw the tiles using grid graphics.
}
\section{Fields}{

\describe{
\item{\code{gr}}{A `GRanges` object containing genomic ranges.}

\item{\code{groupCol}}{Name of metadata column in `gr` used
to define grouping (y-axis categories).}

\item{\code{groups}}{Vector of unique group labels, in display order.}

\item{\code{y}}{Integer vector mapping each row of `gr` to its
group index (1..N). Used for discrete y positioning.}

\item{\code{coordOriginal}}{Original GRanges object.}

\item{\code{coordCanvas}}{List of per-panel tile coordinates (x0, x1, y0, y1, color).}

\item{\code{aesthetics}}{Plotting aesthetics (border, lwd).}

\item{\code{defaultAesthetics}}{Default non-fill aesthetics for tiles.}
}}

